# Repeat Speaker - 环境变量配置示例
# 复制此文件为 .env 并填入实际值

# ============================================
# DeepSeek API 配置（必需）
# ============================================
# 用于获取单词详细信息（音标、释义、例句等）
# 获取地址: https://platform.deepseek.com/
DEEPSEEK_API_KEY=your_deepseek_api_key_here

# ============================================
# SMTP 邮件配置（可选）
# ============================================
# 用于"忘记密码"功能发送验证码
# 如果不配置，忘记密码功能将不可用

# SMTP 服务器地址（QQ邮箱示例）
SMTP_HOST=WordPlayer@outlook.com

# SMTP 端口（SSL: 465, TLS: 587）
SMTP_PORT=465

# 发件人邮箱地址
SMTP_USER=your_email@qq.com

# 邮箱授权码（不是邮箱密码！）
# QQ邮箱获取方式: 设置 -> 账户 -> POP3/IMAP/SMTP/Exchange/CardDAV/CalDAV服务 -> 生成授权码
SMTP_PASSWORD=your_smtp_authorization_code

# 发件人显示名称（可选，默认使用 SMTP_USER）
SMTP_SENDER=your_email@qq.com

# 是否使用 SSL（true/false）
SMTP_USE_SSL=true

# ============================================
# 安全配置（重要）
# ============================================
# 用于加密 session 和 token
# 生产环境必须修改为随机字符串！
# 生成方式: python3 -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=dev-secret-key-change-in-production

# ============================================
# Token 配置（可选）
# ============================================
# 登录 token 有效期（天）
TOKEN_EXPIRE_DAYS=30

# ============================================
# 验证码配置（可选）
# ============================================
# 验证码有效期（分钟）
CODE_EXPIRE_MINUTES=5

# 验证码重发间隔（秒）
CODE_RESEND_SECONDS=60

# ============================================
# 数据库配置（可选）
# ============================================
# 数据库文件路径（默认: data/dictation.db）
# DATABASE_PATH=/path/to/your/database.db

# ============================================
# 使用说明
# ============================================
# 1. 复制此文件: cp .env.example .env
# 2. 编辑 .env 文件，填入实际配置值
# 3. 重启应用使配置生效
#
# 注意事项:
# - .env 文件不会被提交到 Git（已在 .gitignore 中）
# - 生产环境务必修改 SECRET_KEY
# - SMTP 配置是可选的，不配置不影响核心功能
# - DEEPSEEK_API_KEY 是必需的，否则无法获取单词详细信息
